environment: prod

replicaCount: 2

service:
  type: ClusterIP

env:
  CANTALOUPE_S3SOURCE_ENDPOINT: ""

initContainers:
  minio:
    enabled: false

resources:
  requests:
    cpu: 250m
    memory: 768Mi
  limits:
    cpu: "1"
    memory: 1536Mi

persistence:
  enabled: false
  size: 10Gi
